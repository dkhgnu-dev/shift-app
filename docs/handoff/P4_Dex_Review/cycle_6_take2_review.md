# Cycle 6 Take2 P4 DIFFレビュー

## 判定

**P4 OK。main統合可。**

ブロッカー、回帰、指示範囲外の変更は確認されませんでした。

## レビュー対象

- Branch: `cc-cycle6`
- Target HEAD: `75f23319561b349b1ac0df307d5c62fd8e22de23`
- 実装commit: `fc5e90a`
- 比較元: `2ec953e`
- 対象: 左右フロートボタンのTake2修正、関連テスト、バージョン表示、handoff

## DIFF確認

前回の差戻し4件はすべて解消されています。

1. 320pxでのボタン重複
   - `min()`と`calc()`による動的クランプで、左右ボタン間に最低8pxを確保しています。
2. タブ復帰時の再計測
   - `activeTab`がEffect依存に追加され、シフト表へ戻った際に表示状態を再計測します。
3. 非表示ボタンのキーボード到達
   - `disabled`、`tabIndex=-1`、`aria-hidden=true`が同期しています。
4. テスト時のprototype汚染
   - descriptor保存と`try/finally`で、テスト後に確実に復元します。

生成ロジック、保存、API、DBへの変更はありません。`git diff --check`も問題ありません。

## 自動検証

- `npm --prefix frontend test`: 21/21 PASS（連続2回）
- `node frontend/test_time_utils.mjs`: 33/33 PASS
- `npm --prefix frontend run build`: PASS
- ブラウザconsole warning/error: 0件

## 実ブラウザ検証

320 / 375 / 768 / 769 / 1280pxで確認しました。

| 幅 | 右移動 | 左戻り | ボタン間隔 | 右端判定 |
|---:|---:|---:|---:|---|
| 320 | 350px | 0px | 8px | 正常 |
| 375 | 350px | 0px | 26px | 正常 |
| 768 | 350px | 0px | 419px | 正常 |
| 769 | 350px | 0px | 117px | 正常 |
| 1280 | 350px | 0px | 628px | 正常 |

全幅で以下を確認しました。

- 初期位置: 左ボタン無効・右ボタン有効
- 中間位置: 左右ボタン有効
- 右端: 左ボタン有効・右ボタン無効
- 無効ボタン: `disabled=true`、`tabIndex=-1`、`aria-hidden=true`
- 左右操作: 右へ350px移動し、左操作で0pxへ復帰
- 固定列: 横スクロール後もドラッグ列・氏名列の位置を維持
- 固定ヘッダー: 縦430pxスクロール後も位置を維持
- タブ復帰: 従業員管理から全体シフト表へ戻るとscrollLeft=0で再計測
- 769px境界: 768px用36pxボタンからPC用46pxボタンへ正常に切替

320pxでは左ボタンが固定氏名列の右端へ6pxだけ重なりますが、氏名・属性・集計表示や操作を遮らず、左右ボタン同士も重なりません。画面上も許容範囲と判断しました。

## 独立補助レビュー

デクスクルーを使用しました。理由は、前回4件の修正を静的DIFF・テストの観点から並行確認し、Dex本体が実ブラウザ座標検証へ集中するためです。

補助レビューもFindingなし、P4 OK提案でした。正式判定はDex本体のDIFF・自動テスト・5幅実機確認を合わせて行っています。

## Kazumax確認レベル

**確認不要**

見た目を含む指定5幅の実ブラウザ確認までDex側で完了しています。
