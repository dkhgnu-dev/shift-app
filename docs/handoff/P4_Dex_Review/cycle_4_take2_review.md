[C4 Take2: Dex(P4) -> Team]

# Cycle 4 Take2 P4再レビュー

## 判定

**OK**

レビュー対象:

- branch: `cc-cycle4`
- 実装commit: `532f291`
- 報告書を含む確認HEAD: `141cd2d`
- Take2比較元: `5cab0b7`

## Findings

リリースを止める問題はありません。

前回指摘した以下の2点は解消されています。

1. デフォルトリセット確定時に `setGeneratedResult(null)` が実行され、古い従業員構成に対応した生成済みシフトが残らない。
2. 従業員管理のスマホカード・PCテーブルで、上位3名だけを鍵持ちとして識別できる。

## Dex検証

- `npm --prefix frontend test`: 13件PASS。
- `node frontend/test_time_utils.mjs`: 33件PASS。
- `npm --prefix frontend run build`: 成功。
- バージョン: PC・モバイルとも `v4.20`。
- 320 / 375 / 768 / 769 / 1280pxで実画面を確認。
- 全幅で鍵持ちバッジは3件。T.S.以降には表示なし。
- 769px以上のPC表は24行。
- 画面全体の横はみ出し、ボタン重なり、ブラウザconsole error/warnなし。
- 769pxちょうどではPC表が密になり鍵持ちバッジが折り返されるが、表示欠落・重なり・操作不能はない。

## 非ブロッキングの改善余地

恒久テストはK.D. / N.E. / N.K. / T.S.と鍵持ちバッジ3件を確認していますが、従業員総数が24名であることを直接assertしていません。

現行実装が `INITIAL_DATA` 全件を復元することと、Dex実画面でPC表24行を確認したため、今回のOK判定には影響しません。今後リセットテストを触る際は、保存後の従業員配列長または一覧行数も確認すると回帰検知がさらに明確になります。

## サブレビュー利用判断

不使用。Take2差分が前回指摘した2領域に限定され、Dex単独でDIFF・自動テスト・実画面を一続きで再現確認できたため。

## Kazumax確認レベル

**確認不要**

Dex側で状態遷移とレスポンシブ表示まで確認済みです。

